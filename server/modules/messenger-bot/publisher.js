/*const api = require('../facebook-api');

module.exports = exports = {
	publishMessage
};

async function publishMessage({event, text}) {
	return api.sendMessage({
		recipientId: event.sender.id,
		text: text
	});
}*/